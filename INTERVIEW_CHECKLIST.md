## 과제 제출 전 체크리스트 (면접관 관점 포함)

이 문서는 **면접 과제 제출 직전**에 “실제로 돌려보고, 근거를 남기고, 질문을 대비”하기 위한 체크리스트입니다.

---

### 1) 5분 데모 시나리오(면접관이 가장 먼저 보는 것)

- **로컬 기동(한 줄)**
  - [ ] `copy .env.example .env`
  - [ ] `docker compose up --build` 만으로 전부 올라오는지
- **접속**
  - [ ] Frontend: `http://localhost:5173/admin/storage`
  - [ ] Backend: `http://localhost:8080/api/tenants`
  - [ ] Nextcloud: `http://localhost:8081/status.php` 에서 `"installed": true`
- **멀티 테넌트 분리 확인**
  - [ ] tenant-a 선택 시 tenant-a 유저만 보이는지
  - [ ] tenant-b 선택 시 tenant-b 유저만 보이는지
- **실시간 갱신(Socket.IO)**
  - [ ] 용량 변경(업로드/삭제/파일 생성) 후 **자동 갱신**되는지
  - [ ] 갱신이 “전체 테넌트”가 아니라 **해당 tenantId만** 갱신되는지

---

### 2) 기능 요구사항 충족 체크(명세 기반)

- **화면(`/admin/storage`)**
  - [ ] 회사 선택 select box
  - [ ] 테이블 컬럼: userId / used(MB) / quota(MB) / usage(%) / progress bar
  - [ ] progress bar가 회색+빨간색 조합으로 표시되는지
  - [ ] `usagePercent` 소수점 표기 규칙이 일관적인지(현재: **소수점 2자리**)
- **API 응답 필드**
  - [ ] `tenantId`
  - [ ] `userId`
  - [ ] `usedBytes`
  - [ ] `quotaBytes`
  - [ ] `usagePercent`
  - [ ] `lastCollectedAt`
- **Nextcloud 연동**
  - [ ] 그룹(tenant-a/tenant-b) ↔ 테넌트 매핑이 동작하는지
  - [ ] 유저 quota 100MB가 적용되는지

---

### 3) 에러/장애 대응(면접관이 “운영 관점” 질문하는 포인트)

- **Nextcloud 장애 시나리오**
  - [ ] 인증 실패(토큰/패스워드 변경) 시 Backend가 “적절히 실패”하는지
    - 기대: 5xx 무한폭발 대신, 로그/응답이 명확(예: 502/503 또는 의미있는 에러 메시지)
  - [ ] Nextcloud 일시 다운 시, 스케줄러가 죽지 않고 복구되는지
- **0 값/엣지케이스**
  - [ ] used=0 또는 quota=0 인 유저도 화면이 깨지지 않는지
  - [ ] quota=0 일 때 usagePercent 계산이 NaN/Infinity가 되지 않는지

---

### 4) 성능/부하(“왜 캐싱이 필요한가”를 설명할 수 있어야 함)

- **캐싱**
  - [ ] 동일 tenantId에 대해 연속 조회 시 Nextcloud를 과도하게 호출하지 않는지(캐시 적중)
  - [ ] 캐시 만료/갱신 주기가 합리적인지(현재 기본 30초)
- **N+1 호출**
  - [ ] 그룹 멤버 수가 많아질 때(예: 1000명) 호출/응답 시간이 어떻게 변하는지 설명 가능
  - [ ] 병렬화/배치/레이트리밋/백오프 등 개선 방향을 말할 수 있는지

---

### 5) 보안(“하드코딩 금지” 외에도 면접관이 보는 지점)

- **비밀정보 관리**
  - [ ] Nextcloud 인증정보가 코드/리포지토리에 하드코딩되지 않았는지
  - [ ] `.env`가 커밋되지 않도록 되어 있는지
- **CORS/Origin**
  - [ ] 개발 편의(`*`) 설정이 과제 범위에 적절한지, 운영 시 변경 포인트를 설명할 수 있는지
- **권한/인증**
  - [ ] 관리자 페이지가 실제 서비스라면 인증이 필요하다는 점을 명확히 인지/설명 가능한지

---

### 6) 코드 품질/구조(“왜 이렇게 설계했나” 질문 대비)

- **Backend**
  - [ ] 도메인(tenant/usage/nextcloud client) 책임 분리가 자연스러운지
  - [ ] API 계약/DTO가 명확한지(필드명/단위/시간 포맷)
  - [ ] 예외 처리/로그 레벨이 적절한지(민감정보 노출 금지)
- **Frontend**
  - [ ] API 호출/상태관리/표시 로직이 과도하게 뒤엉키지 않았는지
  - [ ] socket 연결 lifecycle(연결/해제)이 누수 없이 동작하는지

---

### 7) 테스트(있으면 강력한 가산점)

- **스모크 테스트**
  - [ ] `GET /api/tenants` 응답 확인
  - [ ] `GET /api/tenants/{id}/usage` 응답 확인
- **E2E(선택/가산점)**
  - [ ] Selenium으로 `/admin/storage` 접근 → tenant 선택 → 테이블 렌더링까지 자동화
- **장애 테스트**
  - [ ] Nextcloud 비번 오입력 시(환경변수) Backend 동작 확인

---

### 8) 문서(README 외 “면접관이 읽기 좋은” 형태)

- **README**
  - [ ] 실행 방법이 1~2분 내 따라할 수 있게 되어 있는지
  - [ ] 자동 init(Nextcloud/DB) 동작과 끄는 방법이 명확한지
- **데모 가이드**
  - [ ] “무엇을 보여주면 되는지” 순서가 적혀 있는지(5분 데모 시나리오)
- **트레이드오프**
  - [ ] 이번 과제에서 단순화한 부분과(예: auth 미구현) 확장 방향을 적어두었는지

---

### 9) 제출 전 최종 점검(실수 방지)

- **재현성**
  - [ ] 기존 컨테이너/볼륨이 없어도(새 PC) 동일하게 뜬다는 근거가 있는지
- **정리**
  - [ ] 불필요한 로그/디버그 출력이 남아있지 않은지
  - [ ] 민감정보(비번/토큰)가 출력/커밋되지 않았는지

